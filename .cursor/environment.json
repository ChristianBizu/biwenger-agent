{
  "name": "Biwenger Agent",
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements.txt pytest"
}
